Importer - eksporter z/do CouchDB i MongoDB w php.

Po dodaniu pliku w formacie JSON przez formularz, dokument pojawia sie na liscie dokumentu pod nazwa bazy, a pod konkretnymi dokumentami wyswietlaja sie mozliwe operacje.
